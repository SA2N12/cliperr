# Reverse proxy HTTPS (certificat Let's Encrypt automatique).
# Le domaine vient de la variable d'env DOMAIN (.env).
# Pas de directive request_body : Caddy n'impose AUCUNE limite de taille par
# défaut (les uploads vidéo volumineux passent en streaming).
{$DOMAIN} {
	reverse_proxy app:8080
}
